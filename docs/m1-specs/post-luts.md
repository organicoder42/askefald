# Task: post stack + LUTs

YOUR FILES: `src/graphics/post.ts` and `src/graphics/luts.ts`

BEFORE WRITING CODE: read node_modules/postprocessing/build/index.d.ts (and source where needed: node_modules/postprocessing/src/...) and node_modules/n8ao/README.md + its type defs, to verify EVERY class/option you use. Critical things to confirm, do not guess: EffectComposer constructor options (frameBufferType: THREE.HalfFloatType), ToneMappingEffect + ToneMappingMode.AGX availability and how exposure is applied (does it read renderer.toneMappingExposure? check src/effects/ToneMappingEffect.js and its shader), LUT3DEffect + LookupTexture (createNeutral?), SMAAEffect constructor, BloomEffect options (mipmapBlur, luminanceThreshold, intensity), GodRaysEffect(camera, lightSourceMesh, opts), DepthOfFieldEffect, ChromaticAberrationEffect (offset Vector2), VignetteEffect, NoiseEffect (premultiply), N8AOPostPass(scene, camera, w, h) + its configuration fields (aoRadius, intensity, halfRes, etc.) and whether it must come right after RenderPass.

TONE MAPPING ARCHITECTURE (verify, then implement): the Engine currently sets renderer.toneMapping = AgXToneMapping. With pmndrs postprocessing the scene renders into HalfFloat buffers (three skips tone mapping + sRGB conversion when rendering to a render target), so the chain must end with explicit tone mapping: in createPostStack, set engine.renderer.toneMapping = THREE.NoToneMapping (leave outputColorSpace SRGB) and add ToneMappingEffect with mode AGX near the END of the chain. Exposure: confirm mechanism — if ToneMappingEffect uses renderer.toneMappingExposure, drive that; if not, find its exposure property. setExposure(target, immediate) lerps exposure toward target with a ~2 s exponential time constant inside render(dt) (§6.8 "eyes adjusting").

CHAIN (order matters): RenderPass(scene,camera) → N8AOPostPass (configure from quality: halfRes, aoRadius ≈ 2.0, distanceFalloff ≈ 1.0, intensity ≈ 2.0 — subtle, the fog carries the mood) → EffectPass(camera, SMAAEffect, GodRaysEffect-if-source, BloomEffect({mipmapBlur:true, luminanceThreshold:0.8, intensity:0.35}), ChromaticAberrationEffect(offset ≤0.0008)) → EffectPass(camera, DepthOfFieldEffect) with pass.enabled=false (dialogue only) → EffectPass(camera, ToneMappingEffect(AGX), LUT3DEffect(actLUT), VignetteEffect(~0.25 darkness, offset ~0.3), NoiseEffect premultiply at ~0.04 opacity). Verify effects-per-pass ordering constraints from postprocessing docs (effects with convolution like Bloom/DoF/GodRays/SMAA may have restrictions about sharing an EffectPass — if a combination throws, split into more passes; document what you did).

- GodRays: opts.godRaysSource may be null → skip. Keep it SUBTLE: density/decay/weight tuned low (§6.2 "subtle").
- setAct(act): swap LUT3DEffect lut to generateActLUT(ACT_CONFIGS[act].grade) and call setExposure(ACT_CONFIGS[act].exposure).
- applyQuality(q): N8AO pass.enabled = q.aoEnabled, halfRes per q.aoHalfRes, godrays pass/effect enabled = q.godRays, store q.dofAllowed (setDofEnabled is a no-op unless allowed).
- setSize(w,h): composer.setSize(w,h) (+ anything n8ao needs).
- render(dt): advance exposure lerp, then composer.render(dt).
- dispose(): composer.dispose() and LUT textures.
- The factory must NOT touch engine loop wiring (caller does engine.setRenderFn / onResize). It MAY set renderer.toneMapping as described.

LUTS (luts.ts): generateActLUT(grade, size=33) → postprocessing LookupTexture. Start from LookupTexture.createNeutral(size) (verify exists; else build the data array yourself). Per texel (display-referred 0..1 RGB): 1) temperature: r += temp*0.04, b -= temp*0.05 (temp = grade.temperature, subtle); 2) saturation about Rec.709 luma; 3) contrast about pivot 0.435: c' = pivot + (c-pivot)*grade.contrast; 4) split-toning: shadowW = pow(1-luma, 2.2), highW = pow(luma, 2.2); blend rgb toward grade.shadowTint by shadowW*0.10 and toward grade.highlightTint by highW*0.12. The tints must be in display-referred sRGB here — note THREE.Color(hex) converts to LINEAR working space by default in three@0.184; VERIFY the color-management semantics and obtain the sRGB component values correctly (e.g. via Color#getRGB with a color space argument, or by parsing hex yourself), and document your reasoning in a comment. Clamp 0..1. Cache by GradeConfig object identity (WeakMap) + size.

VERIFY: tsc clean for both files. integrationNotes: exact wiring for main.ts (engine.setRenderFn(stack.render-bound), engine.onResize, renderer.toneMapping ownership, what godRaysSource mesh must be: emissive/basic mesh positioned at sun, in-scene).
